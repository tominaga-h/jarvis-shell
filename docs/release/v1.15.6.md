# v1.15.6 リリースノート

> 内部構造の大規模リファクタリングを中心としたリリースです。jarvish の振る舞い（補完・実行エンジン・AI 連携・ストレージ）は一切変更しておらず、肥大化していた巨大モジュールを責務単位のディレクトリ構成へ分割しました。あわせて README のデモ GIF を [vhs](https://github.com/charmbracelet/vhs) で再生成し、記録用の `.tape` をリポジトリへ同梱しています。

## 主な機能

機能追加はありません。

## バグ修正

バグ修正はありません（振る舞いに影響する変更を含まないリリースです）。

## 内部改善

### 巨大モジュールの責務単位分割

CLAUDE.md のコーディング規約「各ファイル（モジュール）は単一かつ明確な責務を持つ」「巨大ファイルは機能単位のサブモジュールへ再編する」に沿って、1,000 行を超えていたモジュール群をディレクトリ化しました。公開 API・実行時の振る舞いは変更していません。

- `src/cli/completer/` — 補完プロバイダを機能単位へ分割
  - `carapace.rs` (1,846 行) → `carapace/{core,binary,candidates,parsing,tests}.rs`
  - `registry_provider.rs` (1,567 行) → `registry_provider/{core,candidates,conditions,filtering,tests}.rs`
  - `zsh_bridge.rs` (3,491 行) → `zsh_bridge/{core,lifecycle,output_parse,capture_script,tests}.rs`
  - `zsh_daemon.rs` (2,593 行) → `zsh_daemon/{core,lifecycle,pty_io,request_framing,tests}.rs`
- `src/shell/` — `mod.rs` (1,679 行) から `construction.rs` / `run.rs` / `reload.rs` / `restart.rs` / `prewarm.rs` を切り出し、`rc.rs` (1,447 行) を `rc/{resolution,file_reading,parsing,execution,options,template,tests}.rs` へ分割
- `src/engine/` — `dispatch/mod.rs` を `{parse,builtin,external,expansion}.rs` へ、`parser/mod.rs` を `{command_list,pipeline,simple_command,redirects,ai_filter,types}.rs` へ、`builtins/update.rs` (837 行) を `update/{version,release,local_binary,homebrew,flag_file}.rs` へ分割
- `src/ai/client/` — `mod.rs` から `core.rs` / `conversation.rs` / `input_processing.rs` / `investigation.rs` / `config_update.rs` を切り出し
- `src/config/` — `mod.rs` (736 行) を `types.rs` / `loading.rs` / `tests.rs` へ分割
- `src/storage/` — `mod.rs` を `types.rs` / `database.rs` / `session.rs` / `tests.rs` へ分割
- `src/cli/highlighter/` — `mod.rs` (535 行) を `core.rs` / `token_styling.rs` / `operator_styling.rs` / `quote_styling.rs` / `env_styling.rs` へ分割

各モジュールのテストは対応する `tests.rs` へ移動し、`make check`（fmt / check / clippy / test）は全パスしています。

### コメントのリファクタリング

コード全体のコメントを整理し、「何をしているか」の重複説明を削って「なぜそうしているか」を残す方針へ統一しました。

### デモ GIF の刷新

- README / docs/README_JA.md のデモを `images/demo.gif` へ差し替え（旧 `images/jarvish-demo.gif` は削除）
- 生成スクリプト `images/demo.tape` を同梱し、以後 vhs で再現可能に

## 対応プラットフォーム

- macOS (aarch64 / x86_64)
- Linux (x86_64)

## 既知の制限

- Windows は未対応
- AI 機能の利用には OpenAI API キーが必要
- carapace 外部補完には別途 `carapace` バイナリのインストールが必要
- zsh ブリッジには `zsh` の実行環境が必要
