# v1.8.3 リリースノート

## 主な機能

- なし（内部改善リリース）

## バグ修正

- **テストの安定性向上**: `sigusr1_handler_can_be_reregistered` テストのスリープ時間を 100ms → 200ms に変更し、CI でのフレーキーテストを解消

## 内部改善

- **Claude コマンドに frontmatter を追加**: `.claude/commands/` 配下のコマンドファイルに frontmatter メタデータを追加
- **開発ルールを CLAUDE.md に追加**: 実装→完了の必須フロー（Development Cycle）を明文化
- **Cargo.lock を更新**: 依存パッケージのバージョンを同期
- **不要な設定ファイルを削除**: `.claude/settings.json` のクリーンアップ

## 対応プラットフォーム

- macOS (aarch64 / x86_64)
- Linux (x86_64)

## 既知の制限

- Windows は未対応
- AI 機能の利用には OpenAI API キーが必要
